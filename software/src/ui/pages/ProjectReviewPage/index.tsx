import { FC, jsx } from 'alumina';
import { CommonPageFrame } from '~/ui/components';

export const ProjectReviewPage: FC = () => (
  <CommonPageFrame pageTitle="Project Review">review page</CommonPageFrame>
);
