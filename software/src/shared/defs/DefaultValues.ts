import { IGlobalSettings } from '~/shared/defs/IpcContract';

export const defaultGlobalSettings: IGlobalSettings = {
  useOnlineResources: false,
  useLocalResouces: false,
  localProjectRootFolderPath: '',
};
