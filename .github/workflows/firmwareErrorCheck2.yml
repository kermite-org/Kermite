name: Firmware ErrorCheck 2

on:
  push:
    branches: [master, variants]
    paths:
      - ".github/workflows/firmwareErrorCheck2.yml"
      - "firmware/scripts2/**/*.ts"
      - "firmware/Makefile"
      - "firmware/src/**"

  pull_request:
    branches: [master, variants]
    types: [opened, synchronize]
    paths:
      - "firmware/Makefile"
      - "firmware/src/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: Install Dependencies
        run: sudo apt install gcc-avr avr-libc binutils-avr

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "14.x"

      - uses: bahmutov/npm-install@v1
        with:
          working-directory: ./firmware/scripts2

      - run: yarn checkHardcodedVersions
        working-directory: ./firmware/scripts2

      - run: yarn checkProjectJsonSchemas
        working-directory: ./firmware/scripts2

      - name: Check Firmware Build Errors
        run: yarn buildAll --abortOnError
        working-directory: ./firmware/scripts2

      - name: Check Project IDs
        run: yarn checkProjectIds --failureStatusIfError
        working-directory: ./firmware/scripts2

      - name: Check Preset Profile Schemas
        run: yarn checkPresetProfileSchemas --failureStatusIfError
        working-directory: ./firmware/scripts2
